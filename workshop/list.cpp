#include <iostream>
#include <list>

using namespace std;

int main() {

    // declaring a list using STL
       // list of length 0
       // empty list of length 7 (no elements inserted)

    // using push_back() to populate list 1

    // printing the list using an iterator
        // creating a new iterator
        // iterator will now point at ______ of the list

        // iterator is pointing at the ___________ of the list
    
        // increment iterator
        // iterator is now pointing at the ___________ in the list

    // similarly the entire list can be printed to the terminal

    return 0;
}